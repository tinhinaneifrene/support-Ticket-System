<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.versionx">

    <application
        android:allowBackup="true"
        android:icon="@drawable/support"
        android:label="@string/app_name"
        android:roundIcon="@drawable/support"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Data_base_tickets"></activity>
        <activity android:name=".newClient1" />
        <activity android:name=".SQLiteDataBaseHelper" />
        <activity android:name=".new_client" />
        <activity android:name=".Support" />
        <activity android:name=".New_Ticket" />
        <activity android:name=".EspaceConsultant" />
        <activity android:name=".EspaceClient" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>